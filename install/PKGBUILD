# Maintainer: Rafał Frączek <rafalfr@protonmail.ch>
pkgname=smarttrafficmeter
pkgver=2.1.1208.6780
pkgrel=1
pkgdesc="Software for measuring network usage"
arch=('i686' 'x86_64')
url=""
license=('GPL3')
depends=('boost-libs' 'curl')
makedepends=('gcc' 'git' 'boost' 'curl' 'binutils')
checkdepends=()
optdepends=()
provides=('smarttrafficmeter')
conflicts=()
replaces=()
backup=()
options=()
install=smarttrafficmeter.install
changelog=
source=()
noextract=()
md5sums=()
validpgpkeys=()

# prepare() {
# 	#cd "$pkgname-$pkgver"
# 	#patch -p1 -i "$srcdir/$pkgname-$pkgver.patch"
# }

build() {
	git clone --depth 1 "git@bitbucket.org:rafalfr/smarttrafficmeter.git"
	cd ${srcdir}/smarttrafficmeter/
	make -f SmartTrafficMeter.cbp.mak linux_release
}

# check() {
# 	#cd "$pkgname-$pkgver"
# 	#make -k check
# }

package() {
 	cp ${srcdir}/smarttrafficmeter/bin/Release/smarttrafficmeter ${pkgdir}/
 	cp ${srcdir}/smarttrafficmeter/install/smarttrafficmeter.service ${pkgdir}/
 }
